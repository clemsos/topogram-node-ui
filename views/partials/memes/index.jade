.post.col-sm-4(ng-repeat='post in posts')
    header.post-header
        h2.post-title
          a(href='/meme/{{post._id}}') {{post.title}}
        .post-meta
          i.fa.fa-bullseye
          | Posted on {{post.created_at | date:'medium'}}
        .post-meta
          p {{ post.description}}
        .post-content
    a.btn.btn-primary(href='/meme/{{post._id}}') Read More